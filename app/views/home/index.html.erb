<% if user_signed_in? %>
  <%= link_to 'My Calendar', calendars_path %>
<% else %>
  <%= form_with(model: Calendar.new, url: calendars_path, method: :post) do |form| %>
    <%= form.label :title %>
    <%= form.text_field :title %>
    <%= form.submit 'Create Calendar' %>
  <% end %>
<% end %>
